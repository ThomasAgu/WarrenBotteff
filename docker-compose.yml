services:
  trading-bot:
    build: .
    container_name: binance_trading_bot
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports